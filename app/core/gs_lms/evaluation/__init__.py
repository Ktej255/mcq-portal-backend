"""GS LMS evaluation adapters.

Holds the GS-specific rubric strategy and the background evaluation service that
consume the shared, subject-neutral evaluation core (``app.core.evaluation``).
This package imports the shared core ONLY — never the Optional domain
(Requirement 2 / Property 9).
"""
